# CHECKPOINT 5 STARTER -- rates below are placeholders (0.0). Fill in the
# real published Bedrock on-demand rates (USD per 1M tokens) for all three
# models per the workshop walkthrough, so the cost panel shows real dollars
# instead of zero. CONFIRM AGAINST THE LIVE PRICING PAGE -- rates change.
models:
  us.anthropic.claude-sonnet-4-6:
    label: "Claude Sonnet 4.6 (tier-2, default)"
    input_per_million: 0.0   # TODO(checkpoint 5)
    output_per_million: 0.0  # TODO(checkpoint 5)
  us.anthropic.claude-opus-4-6-v1:
    label: "Claude Opus 4.6 (ceiling)"
    input_per_million: 0.0   # TODO(checkpoint 5)
    output_per_million: 0.0  # TODO(checkpoint 5)
  us.amazon.nova-lite-v1:0:
    label: "Amazon Nova Lite (tier-1, cheap)"
    input_per_million: 0.0   # TODO(checkpoint 5)
    output_per_million: 0.0  # TODO(checkpoint 5)

# Non-model costs (per operation), for the full cost panel later:
gateway:
  invoke_per_call: 0.000001   # placeholder, confirm against Gateway pricing
code_interpreter:
  per_cpu_second: 0.00013     # placeholder, confirm against Code Interpreter pricing
browser:
  per_cpu_second: 0.00013     # placeholder, confirm against Browser pricing
