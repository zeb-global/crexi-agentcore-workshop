.venv/
__pycache__/
*.pyc
node_modules/
cdk.out/
.env
.env.local
agentcore/.env.local
*.egg-info/
.DS_Store

# Vendored pure-Python deps (rebuilt by scripts/vendor-deps.sh, not committed)
services/mcp_market_data/pypdf/
services/mcp_market_data/pypdf-*.dist-info/
services/mcp_market_data/bin/
services/mcp_market_data/.lock

services/legacy_deal_desk/certifi/
services/legacy_deal_desk/jose/
services/legacy_deal_desk/ecdsa/
services/legacy_deal_desk/rsa/
services/legacy_deal_desk/pyasn1/
services/legacy_deal_desk/six.py
services/legacy_deal_desk/bin/
services/legacy_deal_desk/*.dist-info/

# backend runtime
backend/.venv/

# frontend runtime
frontend/node_modules/
frontend/dist/

# Per-participant harness dirs you create by hand per WALKTHROUGH.md -- the
# AgentCore CLI derives a harness's config directory from its registry name,
# not a separate path, so a uniquely-named harness needs its own directory.
# Neither agentcore/ nor app/ is tracked at all on this branch -- you create
# both from scratch (see WALKTHROUGH.md's Phase 0) -- these patterns just
# stop your own local copies from being accidentally committed if you fork
# this branch and start tracking your work.
agentcore/
app/investorAgent_*/
app/brokerAgent_*/
